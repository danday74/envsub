const config = {
  envsub: {
    DEFAULT_OPTIONS: {
      diff: false,
      protect: false,
      syntax: 'default'
    }
  },
  envsubh: {
    DEFAULT_OPTIONS: {
      diff: false
    }
  }
};

module.exports = config;
