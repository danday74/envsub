const config = {
  envsub: {
    DEFAULT_OPTIONS: {
      diff: false,
      protect: false,
      syntax: 'default'
    }
  },
  envsubh: {
    DEFAULT_OPTIONS: {
      diff: false
    }
  },
  regex: '[a-zA-Z_]+[a-zA-Z0-9_]*'
};

module.exports = config;
